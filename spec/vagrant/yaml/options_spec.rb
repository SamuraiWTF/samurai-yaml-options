RSpec.describe Vagrant::Yaml::Options do
  it "has a version number" do
    expect(Vagrant::Yaml::Options::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
